import ChatContainer from "./ChatContainer";
import Chat from "./Chat";

const Main: React.FC = function () {
  return (
    <main>
      <ChatContainer>
        <Chat />
      </ChatContainer>
    </main>
  );
};

export default Main;
